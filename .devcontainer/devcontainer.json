{
  "name": "r1dev",
  "build": {
    // here you can specify which Dockerfile to use for the dev container
    "dockerfile": "Dockerfile.ubuntu-py314",
    "context": ".."
  },
  "remoteUser": "vscode",
  "runArgs": [
    "--hostname", "r1dev", 
    "--name", "r1dev"
],
  "postCreateCommand": "npm install -g @openai/codex"
}
